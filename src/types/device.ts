export interface DeviceInfo {
  hostname: string;
  platform: string;
  release: string;
  arch: string;
  cpus: number;
  total_memory: number;
  os_type: string;
  device_id: string;
  device_fingerprint: string;
} 